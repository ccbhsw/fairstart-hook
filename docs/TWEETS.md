# Tweet Drafts

## Launch Tweet

FairStart Hook is live for the X Layer Build X Hackathon.

It is a Uniswap v4 Hook that gives new pools transparent launch protection:

- dynamic anti-sweep fees
- same-wallet cooldown fees
- early LP removal lock
- public on-chain launch rules

Built on X Layer for @XLayerOfficial @Uniswap @flapdotsh

GitHub:
Demo:
Hook:

## Progress Tweet

Building FairStart Hook for the X Layer Build X Hackathon.

The MVP passes local tests:

- launch-window fee
- cooldown fee
- large-swap fee
- LP removal lock
- rule lock after pool initialization

@XLayerOfficial @Uniswap @flapdotsh

## Demo Tweet

FairStart Hook demo:

1. normal swap gets low fee
2. repeated same-actor swap gets cooldown fee
3. large swap gets anti-sweep fee
4. early LP removal is blocked during launch protection

All rules are enforced by a Uniswap v4 Hook on X Layer.

@XLayerOfficial @Uniswap @flapdotsh
